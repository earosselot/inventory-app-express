extends layout

//{ title: 'Category List', categoryList }

//- block content
//-   h1= title
//-   ul 
//-     -categoryList.sort(function(a, b) {const textA = a.title.toUpperCase(); textB = b.title.toUpperCase(); return (textA < textB) ? -1 : (textA > textB) ? 1 : 0;});
//-     each category in categoryList
//-       li
//-         a(href=category.url) #{category.name}
//-         | (cantidad articulos)
    
//-     else 
//-       li There are no categories.
      